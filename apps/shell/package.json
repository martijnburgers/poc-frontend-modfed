{
  "name": "@poc-mf/shell",
  "version": "0.1.0",
  "private": true,
  "description": "App shell (host) that loads remote modules via Module Federation",
  "scripts": {
    "dev": "rspack serve",
    "build": "rspack build",
    "serve": "serve -s dist -l 3000 --no-clipboard",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@poc-mf/contracts": "*",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@module-federation/enhanced": "^0.8.12",
    "@rspack/cli": "^1.1.8",
    "@rspack/core": "^1.1.8",
    "@rspack/plugin-react-refresh": "^1.0.2",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "react-refresh": "^0.14.2"
  }
}
